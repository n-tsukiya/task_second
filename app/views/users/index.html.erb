<h1>Users#index(ユーザー一覧)</h1>
<p>Find me in app/views/users/index.html.erb</p>
<hr>

<% @users.each do |user| %>
  <p><span>ユーザー名　　: </span><%= link_to user.username, user_path(user.id) %></p>
  <p><span>メールアドレス: </span><%= link_to user.email, user_path(user.id) %></p>
  <hr>
<% end %>